[Unit]
Description=Agent Chatroom Server
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/agent-chatroom
ExecStart=/usr/bin/python3 -m uvicorn server:app --host 0.0.0.0 --port 8765
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
